file structure:
- math
  - matrix operations (all matrices are raw, wrap in a M object to call the operations)
  - M(a).mul(b).add(c).val()
- renderer
  - transform vertices
  - skip rendering vertices behind the camera
  - mesh ordering (distance to point of origin ordering)
  - backface culling
  - triangle render
- loop
  - request animation frame, proper dt handling to deal with lag
  - dt multiply rotations
- controls
  - wasd
  - eq (roll)
  - updownarrows (pitch)
  - r (rotate pitch/yawtowards 0 angles)
- designer
    - draw screen
    - add points (button add point, plus sign listener)
    - add triangles (textarea)
    - rotate mesh
    - save (localstorage)
    - print console.log

meshes:
- spaceship
- space station

space:
- planets
- points of interest
- sectors